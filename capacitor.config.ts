import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.citasfamosas',
  appName: 'citasfamosas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
